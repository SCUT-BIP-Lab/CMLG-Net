task_name = MG_CMLGNet
model_save_dir = {your_save_path}/{task_name}   # your address to save model parameters

modality = RGB,Depth
frames_root = {your_dataset_root}/color_hand,{your_dataset_root}/depth_hand
train_label_file = {your_file_root}/train_UMG_gesture1.csv
eval_label_file_root = {your_file_root}
eval_label_files = test_cross_session_FG_gesture1.csv

frames_per_video = 64

model_path = CMLGNet  # 以src/model为根目录
model_name = Model_CMLGNet

optimizer=adam  # 优化器
